package com.johnny.statusbar_control_eink.ui.theme

import androidx.compose.ui.graphics.Color

val EinkBlack = Color(0xFF000000)
val EinkWhite = Color(0xFFFFFFFF)

// Decorative only (e.g. disabled-state hint) — never load-bearing for meaning,
// since the display may render strictly black-and-white.
val EinkGray = Color(0xFF808080)
